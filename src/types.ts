export interface Member {
  peerId: string,
  stream: any,
}

export interface Pointing {
  peerId: string,
  x: number,
  y: number,
}

export interface Transform {
  x: number,
  y: number,
  scale: number,
}
